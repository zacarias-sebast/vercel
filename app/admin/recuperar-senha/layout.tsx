export default function RecuperarSenhaLayout({
  children,
}: {
  children: React.ReactNode
}) {
  // Layout para página de recuperação de senha - sem proteção de autenticação
  return <>{children}</>
}
